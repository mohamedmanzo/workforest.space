---
import Base from '../layouts/Base.astro';
---

<Base title="Work Forest">
  <section class="hero">
    <div class="hero-badge">Open Source Pattern</div>
    <h1>Work Forest</h1>
    <p class="tagline">A pattern for parallel AI-assisted development</p>
    <p class="hero-desc">
      Enable multiple AI agents to work simultaneously on different parts of your codebase.
      Using git worktrees and structured planning, parallelize development without conflicts.
    </p>
    <div class="hero-cta">
      <a href="/patterns/work-forest" class="btn btn-primary">Get Started</a>
      <a href="https://github.com/joshribakoff/workforest.space" class="btn btn-secondary" target="_blank">
        View on GitHub
      </a>
    </div>
  </section>

  <section class="patterns">
    <h2>Patterns</h2>
    <div class="pattern-grid">
      <a href="/patterns/work-forest" class="pattern-card">
        <div class="pattern-icon">🌲</div>
        <h3>Work Forest</h3>
        <p>The core parallel development pattern using git worktrees</p>
      </a>
      <a href="/patterns/work-clusters" class="pattern-card">
        <div class="pattern-icon">⚡</div>
        <h3>Work Clusters</h3>
        <p>Spawning multiple agents simultaneously on independent tasks</p>
      </a>
      <a href="/patterns/planning" class="pattern-card">
        <div class="pattern-icon">📋</div>
        <h3>Planning Documents</h3>
        <p>Persistent plans that survive context resets across sessions</p>
      </a>
      <a href="/patterns/git-mechanics" class="pattern-card">
        <div class="pattern-icon">⚙️</div>
        <h3>Git Mechanics</h3>
        <p>Underlying worktree and submodule operations</p>
      </a>
    </div>
  </section>

  <section class="quick-start">
    <h2>Quick Start</h2>
    <pre><code># Create a worktree for a feature
git worktree add ../myproject-feature -b feature

# Track in parent repo
git submodule add ../myproject-feature ./myproject-feature

# Now multiple agents can work in parallel
# Each in their own worktree, no conflicts</code></pre>
  </section>
</Base>

<style>
  .hero {
    text-align: center;
    padding: 4rem 0 3rem;
  }

  .hero-badge {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent);
    background: var(--accent-subtle);
    padding: 0.35rem 0.75rem;
    border-radius: 100px;
    margin-bottom: 1.5rem;
  }

  .hero h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, var(--fg) 0%, var(--fg-muted) 50%, var(--accent) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .tagline {
    font-size: 1.25rem;
    color: var(--fg-muted);
    margin-bottom: 1rem;
  }

  .hero-desc {
    max-width: 36rem;
    margin: 0 auto 2rem;
    color: var(--fg-subtle);
    line-height: 1.6;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    font-size: 0.9rem;
    font-weight: 600;
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.15s ease;
  }

  .btn-primary {
    background: var(--accent);
    color: var(--bg);
  }

  .btn-primary:hover {
    background: var(--fg);
    color: var(--bg);
  }

  .btn-secondary {
    background: var(--bg-elevated);
    color: var(--fg-muted);
    border: 1px solid var(--border);
  }

  .btn-secondary:hover {
    color: var(--fg);
    border-color: var(--fg-subtle);
  }

  .patterns {
    padding: 2rem 0;
  }

  .patterns h2 {
    margin-top: 0;
    margin-bottom: 1.5rem;
  }

  .pattern-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  .pattern-card {
    display: block;
    padding: 1.5rem;
    background: var(--bg-elevated);
    border: 1px solid var(--border-subtle);
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .pattern-card:hover {
    border-color: var(--border);
    transform: translateY(-2px);
  }

  .pattern-icon {
    font-size: 1.75rem;
    margin-bottom: 0.75rem;
  }

  .pattern-card h3 {
    font-size: 1.1rem;
    color: var(--fg);
    margin: 0 0 0.5rem;
  }

  .pattern-card p {
    font-size: 0.9rem;
    color: var(--fg-subtle);
    margin: 0;
    line-height: 1.5;
  }

  .quick-start {
    padding: 2rem 0 0;
  }

  .quick-start h2 {
    margin-top: 0;
  }

  @media (max-width: 640px) {
    .hero h1 {
      font-size: 2.5rem;
    }

    .pattern-grid {
      grid-template-columns: 1fr;
    }

    .hero-cta {
      flex-direction: column;
      align-items: center;
    }

    .btn {
      width: 100%;
      max-width: 240px;
      justify-content: center;
    }
  }
</style>
